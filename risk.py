def vol_target_sizing(signal, returns, target_vol=0.10, max_leverage=1.5):
    """Escala por vol realizada pasada (shift 1: sin look-ahead)."""

@dataclass
class CircuitBreaker:
    """NORMAL → BREACHED (flat N barras) → NORMAL. update(equity) → 0 o 1."""